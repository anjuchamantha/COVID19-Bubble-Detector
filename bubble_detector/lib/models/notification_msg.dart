class NotificationMsg {
  String title;
  String body;
  DateTime dateTime;
  double distance;
  bool isDirect;
  NotificationMsg(
      this.title, this.body, this.dateTime, this.distance, this.isDirect);
}
